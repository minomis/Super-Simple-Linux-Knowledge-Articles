Mac commands that may be useful in the future.
---

# Find Mac serial number
ioreg -l | grep -i serialnumber | grep -i platform

# Boot into recovery mode from terminal
sudo nvram recovery-boot-mode=unused

# Connect to a windows file share with credentials on a Mac
smb://<username>:*@<serverNameHere>

# Find files last changed
find /Library/ -newerct '5 seconds ago' ; find ~/ -newerct '5 seconds ago' 

# Find text and replace
sed -i .org 's/<targetText>/<replacementText>/' /<locationPath>/<file.txt>

# Add a firewall exception at the application layer
/usr/libexec/ApplicationFirewall/socketfilterfw --add </pathToApp/test.app>

# How to convert a mobileconfig signed profile to a readable XML file
security cms -D -i </path/to/file.mobileconfig> | xmllint --pretty 1 - > <path/to/new/file.xml>

# How to sign an application
codesign --sign <"Insert Certificate Common Name"> <"location/of/.app"> 
